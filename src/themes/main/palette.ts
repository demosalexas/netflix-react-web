const core = {
  primary: '#E31A13',
  secondary: '#1A1A1A',
};

const typography = {
  primary: 'FFFFFF',
  contrast: '#868D96',
};

const border = {
  default: '#E0E0E0',
};

const palette = {
  core,
  typography,
  border,
};

export default palette;
